import { FormatPatternPlaywrightPipe } from './format-pattern-playwright.pipe';

describe('FormatPatternPlaywrightPipe', () => {
  it('should be defined', () => {
    expect(new FormatPatternPlaywrightPipe()).toBeDefined();
  });
});
